**打开就知道今天是第几周、今天上什么课。**

## 这个版本新增

- **课表**：按周目循环排课 —— N 排（N = 循环周数）× 周一至周日，每格一节。
  每格只有「无」和「自定义」两个选项，点一下就能改。
- **主界面改版**：圈内显示今天要上的科目，圈下面显示「第 N 周」。
- **桌面小组件显示科目**（之前只有周目），并且设置与 App 自动同步，不用再单独设置。
- **App Group 共享存储**：App 和小组件共用一份设置，改完立刻生效。
- 从旧版本升级会**自动迁移**原来的设置，不会丢数据。

## 下载

**`ZhouMu-1.2-unsigned.ipa`** —— 无签名包，用 Sideloadly / AltStore / ESign 等工具自签安装。

需要 **iOS 17 或更高**。

## 安装前必读

- 这个 IPA 里含小组件扩展（`PlugIns/ZhouMuWidget.appex`），**自签工具必须把它一起重新签名**，
  否则小组件用不了。装完长按桌面 ▸「+」搜「周目」，**能搜到就说明签上了**。
- 自签工具重新签名后，**App Group 权限通常会被丢掉**，这时小组件会自动回退成只显示周目 ——
  可以**长按小组件 ▸ 编辑小组件**手动填开学日期，功能不受影响。
- 用免费 Apple ID 自签**同样只有 7 天有效期**，到期重新签一次即可（数据不丢）。
  那些「企业证书」能到 1 年，但属于 Apple 明令禁止的用途，随时会被吊销，不建议用。

## 功能一览

- 打开直接显示今天是第几周、今天上什么课
- 开学日期设置 + 周目循环设置（1–20 周）
- 课表：按周目循环排，N 排 × 周一至周日
- 桌面小组件（小号 / 中号）
- 第一次启动、以及每年 1 月 / 7 月的第一次启动自动弹出设置
- 不联网、不要账号、不收集任何数据

## 计算规则

```
开学第 rawWeek 周 = 距开学天数 / 7 + 1
显示周目 = (rawWeek - 1) % 循环周数 + 1
```

例：开学第 4 周、循环 3 周 → 显示第 1 周，课表也回到第 1 排。

详细说明见仓库的 README 和 CHANGELOG。
