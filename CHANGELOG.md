# 更新日志

## 1.3

- **整体改色**：橙色换成蓝色。浅色模式白底蓝调（`#2563EB`），深色模式黑底蓝调（`#4C8DFF`）。
- **App 图标同步换色**：白蓝图标 + 深色变体（iOS 18+ 在深色模式下自动切换）。
- **新增外观开关**：浅色 / 深色 / 跟随系统，三态切换。
- **课表拆成两张**：原「课表」改名 **晚课表**，新增 **正课表**。两张表互相独立，可以单独关闭。
- **每日节数独立可调**：周一到周日每天可以设 1–12 节（原来的课表每天只有 1 节）。
- **每节可选填上下课时间**：两张表各有一套时间，互不影响。
- **排布方式可选**：每张表都能在「固定」和「按周目轮换」之间切换。
- **首页改成两页**：第一页是圆环 + 三行倒计时（距下节课 / 本节剩余 / 下节是什么）；
  第二页是当天完整课表，当前这节标「进行中」。
- **圆环改成按课程完成进度填充**，课间休息时段环闭合，每节课结束后重置。
- **新增灵动岛 / 锁屏实时活动**：显示当前科目和倒计时，上课前 5 分钟、下课前 5 分钟各提醒一次。
  没填时间的节次无法使用提醒，此时首页圈内会回退显示当天晚课表的科目。
- 从 1.2 升级时**自动迁移**：原来的课表搬进晚课表（每天 1 节、按周目轮换、无时间），设置不丢。

### Apple Watch

- 新增手表应用，**随 iPhone 应用一起安装**（`ZhouMu.app/Watch/ZhouMuWatch.app`）。
- 手表界面：第 N 周大字 + 本周进度条 + 今天的课；自己一套设置。
- 表盘复杂功能四种样式：圆形 / 矩形 / 行内 / 角标。
- 手表应用和 iPhone 应用共用一个 App Group；`Shared/Theme.swift` 补了 watchOS 分支
  （`UIColor(dynamicProvider:)` 在手表上不可用）。
- 手表代码在 [zhoumu-watch](https://github.com/mbhdnysxyv-ux/zhoumu-watch) 另有一份副本。

### 已知限制

- **灵动岛不会在精确时刻自动消失。** iOS 要求活动保持 `active` 才能留在灵动岛，
  而 `end()` 会让它立刻从灵动岛移除（只有锁屏保留到 dismissal 时间）。
  精确到点结束需要 APNs 的 push-to-end，即需要服务器。
  当前行为：课后卡片停在下课时间（系统会因 `staleDate` 把它变暗，倒计时下方标出目标时刻），
**排了后台刷新尽力清理**，并且**打开 App 时立即追平或移除**。

## 1.2

- **新增 App Group 共享存储**：App 和小组件共用一份设置，改了立刻同步。
- **小组件显示今天的科目**，不再只是周目。
- 小组件设置来源改为「优先共享存储、读不到就用小组件自己的配置」——
  自签安装时 App Group 权限通常拿不到，这时自动回退成手动设置，不会变成一个坏掉的小组件。
- 设置里新增/调整：App 里改完设置会立刻通知小组件刷新。
- 从旧版本升级时**自动迁移**原来的设置（开学日期、循环周数、课表），不会丢数据。

## 1.1

- **新增课表**：按周目循环排课，N 排（N = 循环周数）× 周一至周日，每格一节。
- 每格只有「无」和「自定义」两个选项。
- 主界面改为：**圈内显示今天的科目，圈下面显示周目**。
- 空格子（无）时圈内显示「无课」。

## 1.0

- 第一个版本：打开直接显示今天是第几周。
- 开学日期设置 + 周目循环设置（1–20 周）。
- 第一次启动、以及每年 1 月 / 7 月的第一次启动自动弹出设置。
- 桌面小组件（小号 / 中号）。
- 未开学时显示「还有 N 天」；跨天自动刷新。
