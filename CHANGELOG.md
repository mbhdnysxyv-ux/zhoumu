# 更新日志

## 1.2

- **新增 App Group 共享存储**：App 和小组件共用一份设置，改了立刻同步。
- **小组件显示今天的科目**，不再只是周目。
- 小组件设置来源改为「优先共享存储、读不到就用小组件自己的配置」——
  自签安装时 App Group 权限通常拿不到，这时自动回退成手动设置，不会变成一个坏掉的小组件。
- 设置里新增/调整：App 里改完设置会立刻通知小组件刷新。
- 从旧版本升级时**自动迁移**原来的设置（开学日期、循环周数、课表），不会丢数据。

## 1.1

- **新增课表**：按周目循环排课，N 排（N = 循环周数）× 周一至周日，每格一节。
- 每格只有「无」和「自定义」两个选项。
- 主界面改为：**圈内显示今天的科目，圈下面显示周目**。
- 空格子（无）时圈内显示「无课」。

## 1.0

- 第一个版本：打开直接显示今天是第几周。
- 开学日期设置 + 周目循环设置（1–20 周）。
- 第一次启动、以及每年 1 月 / 7 月的第一次启动自动弹出设置。
- 桌面小组件（小号 / 中号）。
- 未开学时显示「还有 N 天」；跨天自动刷新。
